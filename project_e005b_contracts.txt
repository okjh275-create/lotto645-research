========================================================================================
LRP Project E-005B Exact Contract Export
========================================================================================

generated_at_kst: 2026-07-27T22:24:55+09:00
project_root: C:\Users\PC2303\Documents\GitHub\lotto645-research
python_version: 3.13.5
target_files: 11


########################################################################################
FILE: lrp/learning/adaptive_engine.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------

def _clamp(value: float, lower: float, upper: float) -> float

class AdaptiveWeightFactors
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None

class AdaptiveWeightConfig
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None

class AdaptiveWeightEngine
  bases: (none)
  decorators: (none)
  method: __init__(self, config: AdaptiveWeightConfig | None = None) -> None
  method: calculate(self, dataset: AdaptiveWeightDataset) -> tuple[AdaptiveWeight, ...]
  method: _calculate_one(self, *, ranking: StrategyRanking, dataset: AdaptiveWeightDataset) -> dict[str, object]

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Deterministic adaptive-weight calculation for M6-005."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from dataclasses import dataclass, field
    6: import math
    7: 
    8: from .adaptive_models import AdaptiveWeight, AdaptiveWeightDataset
    9: from .ranking import StrategyRanking
   10: 
   11: 
   12: def _clamp(value: float, lower: float, upper: float) -> float:
   13:     return max(lower, min(upper, float(value)))
   14: 
   15: 
   16: @dataclass(frozen=True, slots=True)
   17: class AdaptiveWeightFactors:
   18:     """Factors used to derive one target weight."""
   19: 
   20:     rank_score: float = 0.60
   21:     confidence: float = 0.25
   22:     stability: float = 0.15
   23: 
   24:     def __post_init__(self) -> None:
   25:         values = (self.rank_score, self.confidence, self.stability)
   26:         if any(value < 0.0 for value in values):
   27:             raise ValueError("adaptive weight factors must be non-negative")
   28:         if not math.isclose(sum(values), 1.0, abs_tol=1e-9):
   29:             raise ValueError("adaptive weight factors must sum to 1.0")
   30: 
   31: 
   32: @dataclass(frozen=True, slots=True)
   33: class AdaptiveWeightConfig:
   34:     """Stable RC1 calculation parameters."""
   35: 
   36:     base_weight: float = 1.0
   37:     learning_rate: float = 0.20
   38:     minimum_weight: float = 0.50
   39:     maximum_weight: float = 1.50
   40:     factors: AdaptiveWeightFactors = field(default_factory=AdaptiveWeightFactors)
   41: 
   42:     def __post_init__(self) -> None:
   43:         if self.base_weight <= 0.0:
   44:             raise ValueError("base_weight must be positive")
   45:         if not 0.0 < self.learning_rate <= 1.0:
   46:             raise ValueError("learning_rate must be between 0 and 1")
   47:         if self.minimum_weight <= 0.0:
   48:             raise ValueError("minimum_weight must be positive")
   49:         if self.maximum_weight < self.minimum_weight:
   50:             raise ValueError("maximum_weight must be >= minimum_weight")
   51: 
   52: 
   53: class AdaptiveWeightEngine:
   54:     """Convert rankings into bounded, normalized adaptive weights."""
   55: 
   56:     def __init__(self, config: AdaptiveWeightConfig | None = None) -> None:
   57:         self.config = config or AdaptiveWeightConfig()
   58: 
   59:     def calculate(
   60:         self,
   61:         dataset: AdaptiveWeightDataset,
   62:     ) -> tuple[AdaptiveWeight, ...]:
   63:         if not dataset.rankings:
   64:             return ()
   65: 
   66:         provisional = [
   67:             self._calculate_one(ranking=item, dataset=dataset)
   68:             for item in dataset.rankings
   69:         ]
   70:         total = sum(float(item["current_weight"]) for item in provisional)
   71:         if total <= 0.0:
   72:             raise RuntimeError("adaptive current-weight total must be positive")
   73: 
   74:         results = tuple(
   75:             AdaptiveWeight(
   76:                 **item,
   77:                 normalized_weight=float(item["current_weight"]) / total,
   78:             )
   79:             for item in provisional
   80:         )
   81:         return tuple(
   82:             sorted(
   83:                 results,
   84:                 key=lambda item: (
   85:                     -item.normalized_weight,
   86:                     item.rank_position,
   87:                     item.strategy_type,
   88:                     item.strategy_name,
   89:                 ),
   90:             )
   91:         )
   92: 
   93:     def _calculate_one(
   94:         self,
   95:         *,
   96:         ranking: StrategyRanking,
   97:         dataset: AdaptiveWeightDataset,
   98:     ) -> dict[str, object]:
   99:         factors = self.config.factors
  100:         unit_target = (
  101:             factors.rank_score * ranking.rank_score
  102:             + factors.confidence * ranking.confidence
  103:             + factors.stability * ranking.stability
  104:         )
  105:         target_weight = self.config.minimum_weight + unit_target * (
  106:             self.config.maximum_weight - self.config.minimum_weight
  107:         )
  108:         target_weight = _clamp(
  109:             target_weight,
  110:             self.config.minimum_weight,
  111:             self.config.maximum_weight,
  112:         )
  113: 
  114:         key = (ranking.strategy_type, ranking.strategy_name)
  115:         previous_weight = float(
  116:             dataset.previous_weights.get(key, self.config.base_weight)
  117:         )
  118:         previous_weight = _clamp(
  119:             previous_weight,
  120:             self.config.minimum_weight,
  121:             self.config.maximum_weight,
  122:         )
  123:         current_weight = (
  124:             (1.0 - self.config.learning_rate) * previous_weight
  125:             + self.config.learning_rate * target_weight
  126:         )
  127:         current_weight = _clamp(
  128:             current_weight,
  129:             self.config.minimum_weight,
  130:             self.config.maximum_weight,
  131:         )
  132: 
  133:         return {
  134:             "strategy_type": ranking.strategy_type,
  135:             "strategy_name": ranking.strategy_name,
  136:             "rank_position": ranking.rank_position,
  137:             "rank_score": ranking.rank_score,
  138:             "target_weight": target_weight,
  139:             "previous_weight": previous_weight,
  140:             "current_weight": current_weight,
  141:             "confidence": ranking.confidence,
  142:             "stability": ranking.stability,
  143:             "trend": ranking.trend,
  144:             "sample_count": ranking.sample_count,
  145:             "revision": dataset.revision,
  146:         }


########################################################################################
FILE: lrp/learning/adaptive_models.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------


class AdaptiveWeightDataset
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: strategy_count(self) -> int

class AdaptiveWeight
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None
  method: strategy_key(self) -> StrategyKey
  method: as_dict(self) -> dict[str, object]

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Immutable models for M6-005 adaptive strategy weights."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from dataclasses import dataclass
    6: from typing import Mapping
    7: 
    8: from .ranking import StrategyRanking
    9: 
   10: 
   11: AdaptiveRevision = tuple[int, int]
   12: StrategyKey = tuple[str, str]
   13: 
   14: 
   15: @dataclass(frozen=True, slots=True)
   16: class AdaptiveWeightDataset:
   17:     """Immutable inputs consumed by AdaptiveWeightEngine."""
   18: 
   19:     revision: AdaptiveRevision
   20:     rankings: tuple[StrategyRanking, ...]
   21:     previous_weights: Mapping[StrategyKey, float]
   22: 
   23:     @property
   24:     def strategy_count(self) -> int:
   25:         return len(self.rankings)
   26: 
   27: 
   28: @dataclass(frozen=True, slots=True)
   29: class AdaptiveWeight:
   30:     """Derived adaptive priority for one strategy."""
   31: 
   32:     strategy_type: str
   33:     strategy_name: str
   34:     rank_position: int
   35:     rank_score: float
   36:     target_weight: float
   37:     previous_weight: float
   38:     current_weight: float
   39:     normalized_weight: float
   40:     confidence: float
   41:     stability: float
   42:     trend: str
   43:     sample_count: int
   44:     revision: AdaptiveRevision
   45: 
   46:     def __post_init__(self) -> None:
   47:         if not self.strategy_type.strip():
   48:             raise ValueError("strategy_type must not be empty")
   49:         if not self.strategy_name.strip():
   50:             raise ValueError("strategy_name must not be empty")
   51:         if self.rank_position <= 0:
   52:             raise ValueError("rank_position must be positive")
   53:         if self.sample_count < 0:
   54:             raise ValueError("sample_count must not be negative")
   55:         if self.trend not in {"UP", "DOWN", "FLAT"}:
   56:             raise ValueError("trend must be UP, DOWN, or FLAT")
   57: 
   58:         bounded = (
   59:             self.rank_score,
   60:             self.target_weight,
   61:             self.previous_weight,
   62:             self.current_weight,
   63:             self.normalized_weight,
   64:             self.confidence,
   65:             self.stability,
   66:         )
   67:         if any(value < 0.0 for value in bounded):
   68:             raise ValueError("adaptive weight values must be non-negative")
   69: 
   70:     @property
   71:     def strategy_key(self) -> StrategyKey:
   72:         return (self.strategy_type, self.strategy_name)
   73: 
   74:     def as_dict(self) -> dict[str, object]:
   75:         return {
   76:             "strategy_type": self.strategy_type,
   77:             "strategy_name": self.strategy_name,
   78:             "rank_position": self.rank_position,
   79:             "rank_score": round(self.rank_score, 6),
   80:             "target_weight": round(self.target_weight, 6),
   81:             "previous_weight": round(self.previous_weight, 6),
   82:             "current_weight": round(self.current_weight, 6),
   83:             "normalized_weight": round(self.normalized_weight, 6),
   84:             "confidence": round(self.confidence, 6),
   85:             "stability": round(self.stability, 6),
   86:             "trend": self.trend,
   87:             "sample_count": self.sample_count,
   88:             "revision": list(self.revision),
   89:         }


########################################################################################
FILE: lrp/learning/adaptive_repository.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------


class AdaptiveWeightRepository
  bases: (none)
  decorators: (none)
  method: __init__(self, ranking_repository: RankingRepository) -> None
  method: build_dataset(self, *, rankings: Sequence[StrategyRanking], strategy_type: str | None, history_limit: int) -> AdaptiveWeightDataset
  method: remember(self, *, strategy_type: str | None, history_limit: int, weights: Sequence[AdaptiveWeight]) -> None
  method: latest(self, *, strategy_type: str | None = None, history_limit: int = 100) -> tuple[AdaptiveWeight, ...]
  method: invalidate_cache(self) -> None
  method: _previous_weight_map(self, *, strategy_type: str | None, history_limit: int) -> Mapping[StrategyKey, float]
  method: _required_text(value: str, *, field_name: str) -> str

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Revision-aware read repository for M6-005 adaptive weights."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from typing import Mapping, Sequence
    6: 
    7: from .adaptive_models import AdaptiveWeight, AdaptiveWeightDataset, StrategyKey
    8: from .ranking import StrategyRanking
    9: from .ranking_repository import RankingRepository
   10: 
   11: 
   12: class AdaptiveWeightRepository:
   13:     """Build adaptive datasets and retain only the latest derived weights.
   14: 
   15:     RC1 is intentionally memory-only. No SQLite schema or append-only
   16:     learning record is changed.
   17:     """
   18: 
   19:     def __init__(self, ranking_repository: RankingRepository) -> None:
   20:         self.ranking_repository = ranking_repository
   21:         self._dataset_cache_key: tuple[
   22:             tuple[int, int],
   23:             str | None,
   24:             int,
   25:         ] | None = None
   26:         self._dataset_cache_value: AdaptiveWeightDataset | None = None
   27:         self._latest_weights: dict[
   28:             tuple[str | None, int],
   29:             tuple[AdaptiveWeight, ...],
   30:         ] = {}
   31: 
   32:     def build_dataset(
   33:         self,
   34:         *,
   35:         rankings: Sequence[StrategyRanking],
   36:         strategy_type: str | None,
   37:         history_limit: int,
   38:     ) -> AdaptiveWeightDataset:
   39:         if history_limit <= 0:
   40:             raise ValueError("history_limit must be positive")
   41: 
   42:         normalized_type = (
   43:             None
   44:             if strategy_type is None
   45:             else self._required_text(
   46:                 strategy_type,
   47:                 field_name="strategy_type",
   48:             )
   49:         )
   50:         revision = self.ranking_repository.repository_revision()
   51:         cache_key = (revision, normalized_type, int(history_limit))
   52: 
   53:         if (
   54:             self._dataset_cache_key == cache_key
   55:             and self._dataset_cache_value is not None
   56:         ):
   57:             return self._dataset_cache_value
   58: 
   59:         dataset = AdaptiveWeightDataset(
   60:             revision=revision,
   61:             rankings=tuple(rankings),
   62:             previous_weights=self._previous_weight_map(
   63:                 strategy_type=normalized_type,
   64:                 history_limit=history_limit,
   65:             ),
   66:         )
   67:         self._dataset_cache_key = cache_key
   68:         self._dataset_cache_value = dataset
   69:         return dataset
   70: 
   71:     def remember(
   72:         self,
   73:         *,
   74:         strategy_type: str | None,
   75:         history_limit: int,
   76:         weights: Sequence[AdaptiveWeight],
   77:     ) -> None:
   78:         normalized_type = (
   79:             None
   80:             if strategy_type is None
   81:             else self._required_text(
   82:                 strategy_type,
   83:                 field_name="strategy_type",
   84:             )
   85:         )
   86:         if history_limit <= 0:
   87:             raise ValueError("history_limit must be positive")
   88:         self._latest_weights[(normalized_type, int(history_limit))] = tuple(weights)
   89: 
   90:     def latest(
   91:         self,
   92:         *,
   93:         strategy_type: str | None = None,
   94:         history_limit: int = 100,
   95:     ) -> tuple[AdaptiveWeight, ...]:
   96:         normalized_type = (
   97:             None
   98:             if strategy_type is None
   99:             else self._required_text(
  100:                 strategy_type,
  101:                 field_name="strategy_type",
  102:             )
  103:         )
  104:         return self._latest_weights.get(
  105:             (normalized_type, int(history_limit)),
  106:             (),
  107:         )
  108: 
  109:     def invalidate_cache(self) -> None:
  110:         self._dataset_cache_key = None
  111:         self._dataset_cache_value = None
  112: 
  113:     def _previous_weight_map(
  114:         self,
  115:         *,
  116:         strategy_type: str | None,
  117:         history_limit: int,
  118:     ) -> Mapping[StrategyKey, float]:
  119:         latest = self._latest_weights.get(
  120:             (strategy_type, int(history_limit)),
  121:             (),
  122:         )
  123:         return {
  124:             item.strategy_key: item.current_weight
  125:             for item in latest
  126:         }
  127: 
  128:     @staticmethod
  129:     def _required_text(value: str, *, field_name: str) -> str:
  130:         normalized = str(value).strip()
  131:         if not normalized:
  132:             raise ValueError(f"{field_name} must not be empty")
  133:         return normalized


########################################################################################
FILE: lrp/learning/learning_facade.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------


class LearningFacade
  bases: (none)
  decorators: (none)
  method: __init__(self, *, ranking_repository: RankingRepository, ranking_engine: StrategyRankingEngine, adaptive_repository: AdaptiveWeightRepository, adaptive_engine: AdaptiveWeightEngine) -> None
  method: rank_strategies(self, *, strategy_type: str | None = None, history_limit: int = 100) -> tuple[StrategyRanking, ...]
  method: get_adaptive_weights(self, *, strategy_type: str | None = None, history_limit: int = 100) -> tuple[AdaptiveWeight, ...]
  method: invalidate_derived_caches(self) -> None

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Derived learning-domain orchestration for ranking and adaptive weights."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from .adaptive_engine import AdaptiveWeightEngine
    6: from .adaptive_models import AdaptiveWeight
    7: from .adaptive_repository import AdaptiveWeightRepository
    8: from .ranking import StrategyRanking, StrategyRankingEngine
    9: from .ranking_repository import RankingRepository
   10: 
   11: 
   12: class LearningFacade:
   13:     """Coordinate derived ranking and adaptive-weight layers."""
   14: 
   15:     def __init__(
   16:         self,
   17:         *,
   18:         ranking_repository: RankingRepository,
   19:         ranking_engine: StrategyRankingEngine,
   20:         adaptive_repository: AdaptiveWeightRepository,
   21:         adaptive_engine: AdaptiveWeightEngine,
   22:     ) -> None:
   23:         self.ranking_repository = ranking_repository
   24:         self.ranking_engine = ranking_engine
   25:         self.adaptive_repository = adaptive_repository
   26:         self.adaptive_engine = adaptive_engine
   27: 
   28:     def rank_strategies(
   29:         self,
   30:         *,
   31:         strategy_type: str | None = None,
   32:         history_limit: int = 100,
   33:     ) -> tuple[StrategyRanking, ...]:
   34:         dataset = self.ranking_repository.build_dataset(
   35:             strategy_type=strategy_type,
   36:             history_limit=history_limit,
   37:         )
   38:         return self.ranking_engine.rank(
   39:             dataset.statistics,
   40:             dataset.histories,
   41:         )
   42: 
   43:     def get_adaptive_weights(
   44:         self,
   45:         *,
   46:         strategy_type: str | None = None,
   47:         history_limit: int = 100,
   48:     ) -> tuple[AdaptiveWeight, ...]:
   49:         rankings = self.rank_strategies(
   50:             strategy_type=strategy_type,
   51:             history_limit=history_limit,
   52:         )
   53:         dataset = self.adaptive_repository.build_dataset(
   54:             rankings=rankings,
   55:             strategy_type=strategy_type,
   56:             history_limit=history_limit,
   57:         )
   58:         weights = self.adaptive_engine.calculate(dataset)
   59:         self.adaptive_repository.remember(
   60:             strategy_type=strategy_type,
   61:             history_limit=history_limit,
   62:             weights=weights,
   63:         )
   64:         return weights
   65: 
   66:     def invalidate_derived_caches(self) -> None:
   67:         self.ranking_repository.invalidate_cache()
   68:         self.adaptive_repository.invalidate_cache()


########################################################################################
FILE: lrp/learning/service.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------


class IncrementalReviewSummary
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: as_dict(self) -> dict[str, int | float]

class LearningService
  bases: (none)
  decorators: (none)
  method: __init__(self, repository: LearningRepository, *, ranking_config: RankingConfig | None = None, adaptive_config: AdaptiveWeightConfig | None = None) -> None
  method: run_incremental_review(self, *, round_no: int | None = None, limit: int | None = None, reviewed_at_kst: str | None = None) -> IncrementalReviewSummary
  method: run_strategy_aggregation(self, *, limit: int | None = None, aggregated_at_kst: str | None = None) -> StrategyAggregationSummary
  method: get_strategy_statistics(self, *, strategy_type: str | None = None) -> tuple[StrategyStatistics, ...]
  method: rank_strategies(self, *, strategy_type: str | None = None, history_limit: int = 100) -> tuple[StrategyRanking, ...]
  method: get_adaptive_weights(self, *, strategy_type: str | None = None, history_limit: int = 100) -> tuple[AdaptiveWeight, ...]

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Incremental learning, aggregation, ranking, and adaptive service."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from dataclasses import dataclass
    6: import time
    7: 
    8: from .adaptive_engine import AdaptiveWeightConfig, AdaptiveWeightEngine
    9: from .adaptive_models import AdaptiveWeight
   10: from .adaptive_repository import AdaptiveWeightRepository
   11: from .aggregator import StrategyAggregationSummary, StrategyAggregator
   12: from .evaluator import evaluate_prediction
   13: from .learning_facade import LearningFacade
   14: from .ranking import RankingConfig, StrategyRanking, StrategyRankingEngine
   15: from .ranking_repository import RankingRepository
   16: from .repository import LearningRepository
   17: from .strategy_stats import StrategyStatistics
   18: 
   19: 
   20: @dataclass(frozen=True, slots=True)
   21: class IncrementalReviewSummary:
   22:     scanned: int
   23:     created: int
   24:     skipped: int
   25:     elapsed_seconds: float
   26: 
   27:     def as_dict(self) -> dict[str, int | float]:
   28:         return {
   29:             "scanned": self.scanned,
   30:             "created": self.created,
   31:             "skipped": self.skipped,
   32:             "elapsed_seconds": self.elapsed_seconds,
   33:         }
   34: 
   35: 
   36: class LearningService:
   37:     """Stable entry point for learning workflows."""
   38: 
   39:     def __init__(
   40:         self,
   41:         repository: LearningRepository,
   42:         *,
   43:         ranking_config: RankingConfig | None = None,
   44:         adaptive_config: AdaptiveWeightConfig | None = None,
   45:     ) -> None:
   46:         self.repository = repository
   47:         self.strategy_aggregator = StrategyAggregator(repository)
   48:         self.ranking_repository = RankingRepository(repository)
   49:         self.ranking_engine = StrategyRankingEngine(ranking_config)
   50:         self.adaptive_repository = AdaptiveWeightRepository(
   51:             self.ranking_repository
   52:         )
   53:         self.adaptive_engine = AdaptiveWeightEngine(adaptive_config)
   54:         self.learning_facade = LearningFacade(
   55:             ranking_repository=self.ranking_repository,
   56:             ranking_engine=self.ranking_engine,
   57:             adaptive_repository=self.adaptive_repository,
   58:             adaptive_engine=self.adaptive_engine,
   59:         )
   60: 
   61:     def run_incremental_review(
   62:         self,
   63:         *,
   64:         round_no: int | None = None,
   65:         limit: int | None = None,
   66:         reviewed_at_kst: str | None = None,
   67:     ) -> IncrementalReviewSummary:
   68:         started = time.perf_counter()
   69:         pending = self.repository.pending_predictions_with_results(
   70:             round_no=round_no,
   71:             limit=limit,
   72:         )
   73: 
   74:         created = 0
   75:         skipped = 0
   76:         result_cache = {}
   77: 
   78:         for prediction in pending:
   79:             result = result_cache.get(prediction.round_no)
   80:             if result is None:
   81:                 result = self.repository.get_result(prediction.round_no)
   82:                 if result is None:
   83:                     skipped += 1
   84:                     continue
   85:                 result_cache[prediction.round_no] = result
   86: 
   87:             review = evaluate_prediction(
   88:                 prediction,
   89:                 result,
   90:                 reviewed_at_kst=reviewed_at_kst,
   91:             )
   92:             if self.repository.add_review(review):
   93:                 created += 1
   94:             else:
   95:                 skipped += 1
   96: 
   97:         return IncrementalReviewSummary(
   98:             scanned=len(pending),
   99:             created=created,
  100:             skipped=skipped,
  101:             elapsed_seconds=round(time.perf_counter() - started, 6),
  102:         )
  103: 
  104:     def run_strategy_aggregation(
  105:         self,
  106:         *,
  107:         limit: int | None = None,
  108:         aggregated_at_kst: str | None = None,
  109:     ) -> StrategyAggregationSummary:
  110:         summary = self.strategy_aggregator.run(
  111:             limit=limit,
  112:             aggregated_at_kst=aggregated_at_kst,
  113:         )
  114:         if summary.created_events:
  115:             self.learning_facade.invalidate_derived_caches()
  116:         return summary
  117: 
  118:     def get_strategy_statistics(
  119:         self,
  120:         *,
  121:         strategy_type: str | None = None,
  122:     ) -> tuple[StrategyStatistics, ...]:
  123:         return self.repository.get_strategy_statistics(
  124:             strategy_type=strategy_type
  125:         )
  126: 
  127:     def rank_strategies(
  128:         self,
  129:         *,
  130:         strategy_type: str | None = None,
  131:         history_limit: int = 100,
  132:     ) -> tuple[StrategyRanking, ...]:
  133:         """Return deterministic rankings from current aggregated evidence."""
  134:         return self.learning_facade.rank_strategies(
  135:             strategy_type=strategy_type,
  136:             history_limit=history_limit,
  137:         )
  138: 
  139:     def get_adaptive_weights(
  140:         self,
  141:         *,
  142:         strategy_type: str | None = None,
  143:         history_limit: int = 100,
  144:     ) -> tuple[AdaptiveWeight, ...]:
  145:         """Return bounded and normalized adaptive strategy priorities."""
  146:         return self.learning_facade.get_adaptive_weights(
  147:             strategy_type=strategy_type,
  148:             history_limit=history_limit,
  149:         )


########################################################################################
FILE: lrp/learning/performance.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------

def _required_text(value: object, *, field_name: str) -> str
def _finite_number(value: object, *, field_name: str) -> float
def _unit_interval(value: object, *, field_name: str) -> float
def _non_negative_integer(value: object, *, field_name: str) -> int
def _positive_integer(value: object, *, field_name: str) -> int
def _normalize_window_mapping(values: Mapping[int, float], *, field_name: str, unit_interval: bool) -> dict[int, float]

class StrategyPerformanceSummary
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None
  method: key(self) -> tuple[str, str]
  method: as_dict(self, *, include_history: bool = True) -> dict[str, Any]

class StrategyPerformanceReport
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None
  method: strategy_count(self) -> int
  method: total_samples(self) -> int
  method: total_history_points(self) -> int
  method: get(self, strategy_type: str, strategy_name: str) -> StrategyPerformanceSummary | None
  method: as_dict(self, *, include_history: bool = True) -> dict[str, Any]

class PerformanceAnalyzer
  bases: (none)
  decorators: (none)
  method: __init__(self, repository: RankingRepository, *, ranking_config: RankingConfig | None = None) -> None
  method: analyze(self, *, strategy_type: str | None = None, history_limit: int = 100, generated_at_kst: str | None = None) -> StrategyPerformanceReport
  method: _build_summary(*, ranking: StrategyRanking, statistics: StrategyStatistics, history: tuple[StrategyPerformancePoint, ...]) -> StrategyPerformanceSummary

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Read-only strategy performance analysis for Project E E-005A."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from dataclasses import dataclass, field
    6: from datetime import datetime
    7: import math
    8: from typing import Any, Mapping
    9: from zoneinfo import ZoneInfo
   10: 
   11: from .ranking import (
   12:     RankingConfig,
   13:     StrategyPerformancePoint,
   14:     StrategyRanking,
   15:     StrategyRankingEngine,
   16: )
   17: from .ranking_repository import RankingRepository
   18: from .strategy_stats import StrategyStatistics
   19: 
   20: 
   21: _KST = ZoneInfo("Asia/Seoul")
   22: _VALID_TRENDS = {"UP", "DOWN", "FLAT"}
   23: 
   24: 
   25: def _required_text(value: object, *, field_name: str) -> str:
   26:     if not isinstance(value, str):
   27:         raise ValueError(f"{field_name} must be a string")
   28:     normalized = value.strip()
   29:     if not normalized:
   30:         raise ValueError(f"{field_name} must not be empty")
   31:     return normalized
   32: 
   33: 
   34: def _finite_number(value: object, *, field_name: str) -> float:
   35:     if isinstance(value, bool) or not isinstance(value, (int, float)):
   36:         raise ValueError(f"{field_name} must be numeric")
   37:     normalized = float(value)
   38:     if not math.isfinite(normalized):
   39:         raise ValueError(f"{field_name} must be finite")
   40:     return normalized
   41: 
   42: 
   43: def _unit_interval(value: object, *, field_name: str) -> float:
   44:     normalized = _finite_number(value, field_name=field_name)
   45:     if not 0.0 <= normalized <= 1.0:
   46:         raise ValueError(f"{field_name} must be between 0 and 1")
   47:     return normalized
   48: 
   49: 
   50: def _non_negative_integer(value: object, *, field_name: str) -> int:
   51:     if isinstance(value, bool) or not isinstance(value, int) or value < 0:
   52:         raise ValueError(f"{field_name} must be a non-negative integer")
   53:     return value
   54: 
   55: 
   56: def _positive_integer(value: object, *, field_name: str) -> int:
   57:     if isinstance(value, bool) or not isinstance(value, int) or value <= 0:
   58:         raise ValueError(f"{field_name} must be a positive integer")
   59:     return value
   60: 
   61: 
   62: def _normalize_window_mapping(
   63:     values: Mapping[int, float],
   64:     *,
   65:     field_name: str,
   66:     unit_interval: bool,
   67: ) -> dict[int, float]:
   68:     normalized: dict[int, float] = {}
   69:     for raw_window, raw_value in values.items():
   70:         try:
   71:             window = int(raw_window)
   72:         except (TypeError, ValueError) as exc:
   73:             raise ValueError(f"{field_name} windows must be integers") from exc
   74:         _positive_integer(window, field_name=f"{field_name} window")
   75:         if window in normalized:
   76:             raise ValueError(f"{field_name} contains duplicate windows")
   77:         if unit_interval:
   78:             value = _unit_interval(
   79:                 raw_value,
   80:                 field_name=f"{field_name}[{window}]",
   81:             )
   82:         else:
   83:             value = _finite_number(
   84:                 raw_value,
   85:                 field_name=f"{field_name}[{window}]",
   86:             )
   87:             if not 0.0 <= value <= 6.0:
   88:                 raise ValueError(
   89:                     f"{field_name}[{window}] must be between 0 and 6"
   90:                 )
   91:         normalized[window] = value
   92:     return normalized
   93: 
   94: 
   95: @dataclass(frozen=True, slots=True)
   96: class StrategyPerformanceSummary:
   97:     """Standard read-only performance view for one strategy."""
   98: 
   99:     strategy_type: str
  100:     strategy_name: str
  101:     sample_count: int
  102:     history_count: int
  103:     average_match_count: float
  104:     best_match_count: int
  105:     worst_match_count: int
  106:     average_prediction_score: float
  107:     hit3_plus_rate: float
  108:     prize_rate: float
  109:     confidence: float
  110:     stability: float
  111:     trend: str
  112:     recent_gain: float
  113:     rank_score: float
  114:     rank_position: int
  115:     rolling_matches: Mapping[int, float] = field(default_factory=dict)
  116:     rolling_prize_rates: Mapping[int, float] = field(default_factory=dict)
  117:     history: tuple[StrategyPerformancePoint, ...] = field(default_factory=tuple)
  118: 
  119:     def __post_init__(self) -> None:
  120:         strategy_type = _required_text(
  121:             self.strategy_type,
  122:             field_name="strategy_type",
  123:         ).lower()
  124:         strategy_name = _required_text(
  125:             self.strategy_name,
  126:             field_name="strategy_name",
  127:         )
  128:         sample_count = _non_negative_integer(
  129:             self.sample_count,
  130:             field_name="sample_count",
  131:         )
  132:         history_count = _non_negative_integer(
  133:             self.history_count,
  134:             field_name="history_count",
  135:         )
  136:         average_match_count = _finite_number(
  137:             self.average_match_count,
  138:             field_name="average_match_count",
  139:         )
  140:         if not 0.0 <= average_match_count <= 6.0:
  141:             raise ValueError("average_match_count must be between 0 and 6")
  142:         best_match_count = _non_negative_integer(
  143:             self.best_match_count,
  144:             field_name="best_match_count",
  145:         )
  146:         worst_match_count = _non_negative_integer(
  147:             self.worst_match_count,
  148:             field_name="worst_match_count",
  149:         )
  150:         if best_match_count > 6:
  151:             raise ValueError("best_match_count must be between 0 and 6")
  152:         if worst_match_count > 6:
  153:             raise ValueError("worst_match_count must be between 0 and 6")
  154:         if worst_match_count > best_match_count:
  155:             raise ValueError(
  156:                 "worst_match_count must not exceed best_match_count"
  157:             )
  158:         average_prediction_score = _unit_interval(
  159:             self.average_prediction_score,
  160:             field_name="average_prediction_score",
  161:         )
  162:         hit3_plus_rate = _unit_interval(
  163:             self.hit3_plus_rate,
  164:             field_name="hit3_plus_rate",
  165:         )
  166:         prize_rate = _unit_interval(
  167:             self.prize_rate,
  168:             field_name="prize_rate",
  169:         )
  170:         confidence = _unit_interval(
  171:             self.confidence,
  172:             field_name="confidence",
  173:         )
  174:         stability = _unit_interval(
  175:             self.stability,
  176:             field_name="stability",
  177:         )
  178:         rank_score = _unit_interval(
  179:             self.rank_score,
  180:             field_name="rank_score",
  181:         )
  182:         trend = _required_text(self.trend, field_name="trend").upper()
  183:         if trend not in _VALID_TRENDS:
  184:             raise ValueError("trend must be UP, DOWN, or FLAT")
  185:         recent_gain = _finite_number(
  186:             self.recent_gain,
  187:             field_name="recent_gain",
  188:         )
  189:         rank_position = _positive_integer(
  190:             self.rank_position,
  191:             field_name="rank_position",
  192:         )
  193:         history = tuple(self.history)
  194:         if not all(
  195:             isinstance(point, StrategyPerformancePoint)
  196:             for point in history
  197:         ):
  198:             raise ValueError("history contains invalid performance points")
  199:         if history_count != len(history):
  200:             raise ValueError("history_count must equal history length")
  201:         ordered_history = tuple(
  202:             sorted(
  203:                 history,
  204:                 key=lambda point: (
  205:                     point.round_no,
  206:                     point.prediction_id,
  207:                 ),
  208:             )
  209:         )
  210:         if history != ordered_history:
  211:             raise ValueError("history must be in chronological order")
  212:         observed_matches = tuple(point.match_count for point in history)
  213:         if observed_matches:
  214:             if best_match_count != max(observed_matches):
  215:                 raise ValueError("best_match_count must match history")
  216:             if worst_match_count != min(observed_matches):
  217:                 raise ValueError("worst_match_count must match history")
  218:         elif best_match_count != 0 or worst_match_count != 0:
  219:             raise ValueError(
  220:                 "empty history requires zero best/worst match counts"
  221:             )
  222:         rolling_matches = _normalize_window_mapping(
  223:             self.rolling_matches,
  224:             field_name="rolling_matches",
  225:             unit_interval=False,
  226:         )
  227:         rolling_prize_rates = _normalize_window_mapping(
  228:             self.rolling_prize_rates,
  229:             field_name="rolling_prize_rates",
  230:             unit_interval=True,
  231:         )
  232:         object.__setattr__(self, "strategy_type", strategy_type)
  233:         object.__setattr__(self, "strategy_name", strategy_name)
  234:         object.__setattr__(self, "sample_count", sample_count)
  235:         object.__setattr__(self, "history_count", history_count)
  236:         object.__setattr__(self, "average_match_count", average_match_count)
  237:         object.__setattr__(self, "best_match_count", best_match_count)
  238:         object.__setattr__(self, "worst_match_count", worst_match_count)
  239:         object.__setattr__(
  240:             self,
  241:             "average_prediction_score",
  242:             average_prediction_score,
  243:         )
  244:         object.__setattr__(self, "hit3_plus_rate", hit3_plus_rate)
  245:         object.__setattr__(self, "prize_rate", prize_rate)
  246:         object.__setattr__(self, "confidence", confidence)
  247:         object.__setattr__(self, "stability", stability)
  248:         object.__setattr__(self, "trend", trend)
  249:         object.__setattr__(self, "recent_gain", recent_gain)
  250:         object.__setattr__(self, "rank_score", rank_score)
  251:         object.__setattr__(self, "rank_position", rank_position)
  252:         object.__setattr__(self, "rolling_matches", rolling_matches)
  253:         object.__setattr__(self, "rolling_prize_rates", rolling_prize_rates)
  254:         object.__setattr__(self, "history", history)
  255: 
  256:     @property
  257:     def key(self) -> tuple[str, str]:
  258:         return self.strategy_type, self.strategy_name
  259: 
  260:     def as_dict(self, *, include_history: bool = True) -> dict[str, Any]:
  261:         payload: dict[str, Any] = {
  262:             "strategy_type": self.strategy_type,
  263:             "strategy_name": self.strategy_name,
  264:             "sample_count": self.sample_count,
  265:             "history_count": self.history_count,
  266:             "average_match_count": round(self.average_match_count, 6),
  267:             "best_match_count": self.best_match_count,
  268:             "worst_match_count": self.worst_match_count,
  269:             "average_prediction_score": round(
  270:                 self.average_prediction_score,
  271:                 6,
  272:             ),
  273:             "hit3_plus_rate": round(self.hit3_plus_rate, 6),
  274:             "prize_rate": round(self.prize_rate, 6),
  275:             "confidence": round(self.confidence, 6),
  276:             "stability": round(self.stability, 6),
  277:             "trend": self.trend,
  278:             "recent_gain": round(self.recent_gain, 6),
  279:             "rank_score": round(self.rank_score, 6),
  280:             "rank_position": self.rank_position,
  281:             "rolling_matches": {
  282:                 str(window): round(value, 6)
  283:                 for window, value in sorted(self.rolling_matches.items())
  284:             },
  285:             "rolling_prize_rates": {
  286:                 str(window): round(value, 6)
  287:                 for window, value in sorted(
  288:                     self.rolling_prize_rates.items()
  289:                 )
  290:             },
  291:         }
  292:         if include_history:
  293:             payload["history"] = [
  294:                 {
  295:                     "prediction_id": point.prediction_id,
  296:                     "round_no": point.round_no,
  297:                     "match_count": point.match_count,
  298:                     "prediction_score": round(point.prediction_score, 6),
  299:                     "prize_rank": point.prize_rank,
  300:                 }
  301:                 for point in self.history
  302:             ]
  303:         return payload
  304: 
  305: 
  306: @dataclass(frozen=True, slots=True)
  307: class StrategyPerformanceReport:
  308:     """Immutable E-005A performance-analysis result."""
  309: 
  310:     revision: tuple[int, int]
  311:     strategy_type: str | None
  312:     history_limit: int
  313:     generated_at_kst: str
  314:     summaries: tuple[StrategyPerformanceSummary, ...]
  315:     metadata: Mapping[str, Any] = field(default_factory=dict)
  316: 
  317:     def __post_init__(self) -> None:
  318:         revision = tuple(self.revision)
  319:         if (
  320:             len(revision) != 2
  321:             or any(
  322:                 isinstance(value, bool)
  323:                 or not isinstance(value, int)
  324:                 or value < 0
  325:                 for value in revision
  326:             )
  327:         ):
  328:             raise ValueError(
  329:                 "revision must contain two non-negative integers"
  330:             )
  331:         strategy_type = self.strategy_type
  332:         if strategy_type is not None:
  333:             strategy_type = _required_text(
  334:                 strategy_type,
  335:                 field_name="strategy_type",
  336:             ).lower()
  337:         history_limit = _positive_integer(
  338:             self.history_limit,
  339:             field_name="history_limit",
  340:         )
  341:         generated_at_kst = _required_text(
  342:             self.generated_at_kst,
  343:             field_name="generated_at_kst",
  344:         )
  345:         summaries = tuple(self.summaries)
  346:         if not all(
  347:             isinstance(item, StrategyPerformanceSummary)
  348:             for item in summaries
  349:         ):
  350:             raise ValueError("summaries contains invalid items")
  351:         keys = [item.key for item in summaries]
  352:         if len(keys) != len(set(keys)):
  353:             raise ValueError("summaries contains duplicate strategies")
  354:         expected_order = tuple(
  355:             sorted(
  356:                 summaries,
  357:                 key=lambda item: (
  358:                     item.rank_position,
  359:                     item.strategy_type,
  360:                     item.strategy_name,
  361:                 ),
  362:             )
  363:         )
  364:         if summaries != expected_order:
  365:             raise ValueError("summaries must be ordered by rank position")
  366:         if strategy_type is not None and any(
  367:             item.strategy_type != strategy_type
  368:             for item in summaries
  369:         ):
  370:             raise ValueError(
  371:                 "summary strategy_type does not match report filter"
  372:             )
  373:         if not isinstance(self.metadata, Mapping):
  374:             raise ValueError("metadata must be a mapping")
  375:         object.__setattr__(self, "revision", revision)
  376:         object.__setattr__(self, "strategy_type", strategy_type)
  377:         object.__setattr__(self, "history_limit", history_limit)
  378:         object.__setattr__(self, "generated_at_kst", generated_at_kst)
  379:         object.__setattr__(self, "summaries", summaries)
  380:         object.__setattr__(self, "metadata", dict(self.metadata))
  381: 
  382:     @property
  383:     def strategy_count(self) -> int:
  384:         return len(self.summaries)
  385: 
  386:     @property
  387:     def total_samples(self) -> int:
  388:         return sum(item.sample_count for item in self.summaries)
  389: 
  390:     @property
  391:     def total_history_points(self) -> int:
  392:         return sum(item.history_count for item in self.summaries)
  393: 
  394:     def get(
  395:         self,
  396:         strategy_type: str,
  397:         strategy_name: str,
  398:     ) -> StrategyPerformanceSummary | None:
  399:         key = (
  400:             _required_text(
  401:                 strategy_type,
  402:                 field_name="strategy_type",
  403:             ).lower(),
  404:             _required_text(
  405:                 strategy_name,
  406:                 field_name="strategy_name",
  407:             ),
  408:         )
  409:         return next(
  410:             (
  411:                 item
  412:                 for item in self.summaries
  413:                 if item.key == key
  414:             ),
  415:             None,
  416:         )
  417: 
  418:     def as_dict(self, *, include_history: bool = True) -> dict[str, Any]:
  419:         return {
  420:             "revision": list(self.revision),
  421:             "strategy_type": self.strategy_type,
  422:             "history_limit": self.history_limit,
  423:             "generated_at_kst": self.generated_at_kst,
  424:             "strategy_count": self.strategy_count,
  425:             "total_samples": self.total_samples,
  426:             "total_history_points": self.total_history_points,
  427:             "summaries": [
  428:                 item.as_dict(include_history=include_history)
  429:                 for item in self.summaries
  430:             ],
  431:             "metadata": dict(self.metadata),
  432:         }
  433: 
  434: 
  435: class PerformanceAnalyzer:
  436:     """Build deterministic reports without modifying learning data."""
  437: 
  438:     def __init__(
  439:         self,
  440:         repository: RankingRepository,
  441:         *,
  442:         ranking_config: RankingConfig | None = None,
  443:     ) -> None:
  444:         if not isinstance(repository, RankingRepository):
  445:             raise TypeError("repository must be a RankingRepository")
  446:         self.repository = repository
  447:         self.ranking_engine = StrategyRankingEngine(ranking_config)
  448: 
  449:     def analyze(
  450:         self,
  451:         *,
  452:         strategy_type: str | None = None,
  453:         history_limit: int = 100,
  454:         generated_at_kst: str | None = None,
  455:     ) -> StrategyPerformanceReport:
  456:         history_limit = _positive_integer(
  457:             history_limit,
  458:             field_name="history_limit",
  459:         )
  460:         normalized_type = (
  461:             None
  462:             if strategy_type is None
  463:             else _required_text(
  464:                 strategy_type,
  465:                 field_name="strategy_type",
  466:             ).lower()
  467:         )
  468:         dataset = self.repository.build_dataset(
  469:             strategy_type=normalized_type,
  470:             history_limit=history_limit,
  471:         )
  472:         rankings = self.ranking_engine.rank(
  473:             dataset.statistics,
  474:             dataset.histories,
  475:         )
  476:         statistics_by_key = {
  477:             (item.strategy_type, item.strategy_name): item
  478:             for item in dataset.statistics
  479:         }
  480:         summaries = tuple(
  481:             self._build_summary(
  482:                 ranking=ranking,
  483:                 statistics=statistics_by_key[
  484:                     (
  485:                         ranking.strategy_type,
  486:                         ranking.strategy_name,
  487:                     )
  488:                 ],
  489:                 history=tuple(
  490:                     dataset.histories.get(
  491:                         (
  492:                             ranking.strategy_type,
  493:                             ranking.strategy_name,
  494:                         ),
  495:                         (),
  496:                     )
  497:                 ),
  498:             )
  499:             for ranking in rankings
  500:         )
  501:         timestamp = generated_at_kst
  502:         if timestamp is None:
  503:             timestamp = datetime.now(_KST).isoformat(timespec="seconds")
  504:         else:
  505:             timestamp = _required_text(
  506:                 timestamp,
  507:                 field_name="generated_at_kst",
  508:             )
  509:         return StrategyPerformanceReport(
  510:             revision=dataset.revision,
  511:             strategy_type=normalized_type,
  512:             history_limit=history_limit,
  513:             generated_at_kst=timestamp,
  514:             summaries=summaries,
  515:             metadata={
  516:                 "source": "lrp.learning",
  517:                 "analyzer": "E-005A",
  518:                 "read_only": True,
  519:                 "history_order": "chronological",
  520:                 "ranking_windows": list(
  521:                     self.ranking_engine.config.windows
  522:                 ),
  523:                 "trend_short_window": (
  524:                     self.ranking_engine.config.trend_short_window
  525:                 ),
  526:                 "trend_long_window": (
  527:                     self.ranking_engine.config.trend_long_window
  528:                 ),
  529:                 "trend_threshold": (
  530:                     self.ranking_engine.config.trend_threshold
  531:                 ),
  532:             },
  533:         )
  534: 
  535:     @staticmethod
  536:     def _build_summary(
  537:         *,
  538:         ranking: StrategyRanking,
  539:         statistics: StrategyStatistics,
  540:         history: tuple[StrategyPerformancePoint, ...],
  541:     ) -> StrategyPerformanceSummary:
  542:         match_values = tuple(point.match_count for point in history)
  543:         return StrategyPerformanceSummary(
  544:             strategy_type=ranking.strategy_type,
  545:             strategy_name=ranking.strategy_name,
  546:             sample_count=statistics.sample_count,
  547:             history_count=len(history),
  548:             average_match_count=statistics.average_match_count,
  549:             best_match_count=max(match_values) if match_values else 0,
  550:             worst_match_count=min(match_values) if match_values else 0,
  551:             average_prediction_score=(
  552:                 statistics.average_prediction_score
  553:             ),
  554:             hit3_plus_rate=statistics.hit3_plus_rate,
  555:             prize_rate=statistics.prize_rate,
  556:             confidence=ranking.confidence,
  557:             stability=ranking.stability,
  558:             trend=ranking.trend,
  559:             recent_gain=ranking.recent_gain,
  560:             rank_score=ranking.rank_score,
  561:             rank_position=ranking.rank_position,
  562:             rolling_matches=dict(ranking.rolling_matches),
  563:             rolling_prize_rates=dict(ranking.rolling_prize_rates),
  564:             history=history,
  565:         )


########################################################################################
FILE: lrp/learning/ranking.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------

def _clamp(value: float, lower: float = 0.0, upper: float = 1.0) -> float
def _safe_mean(values: Sequence[float]) -> float

class StrategyPerformancePoint
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None

class RankingWeights
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None

class RankingConfig
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None

class StrategyRanking
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: __post_init__(self) -> None
  method: as_dict(self) -> dict[str, object]

class StrategyRankingEngine
  bases: (none)
  decorators: (none)
  method: __init__(self, config: RankingConfig | None = None) -> None
  method: rank(self, statistics: Sequence[StrategyStatistics], histories: Mapping[tuple[str, str], Sequence[StrategyPerformancePoint]]) -> tuple[StrategyRanking, ...]
  method: _calculate_one(self, *, statistics: StrategyStatistics, history: Sequence[StrategyPerformancePoint]) -> dict[str, object]
  method: _confidence(self, sample_count: int) -> float
  method: _stability(match_values: Sequence[float]) -> float
  method: _trend(self, match_values: Sequence[float]) -> tuple[str, float]

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Strategy ranking models and deterministic ranking engine.
    2: 
    3: M6-004 keeps ranking as a derived calculation layer.  No ranking table is
    4: required: the engine consumes incrementally accumulated StrategyStatistics
    5: plus a bounded recent-performance window supplied by the repository.
    6: """
    7: 
    8: from __future__ import annotations
    9: 
   10: from dataclasses import dataclass, field
   11: import math
   12: from statistics import fmean, pstdev
   13: from typing import Iterable, Mapping, Sequence
   14: 
   15: from .strategy_stats import StrategyStatistics
   16: 
   17: 
   18: _VALID_TRENDS = {"UP", "DOWN", "FLAT"}
   19: 
   20: 
   21: def _clamp(value: float, lower: float = 0.0, upper: float = 1.0) -> float:
   22:     return max(lower, min(upper, float(value)))
   23: 
   24: 
   25: def _safe_mean(values: Sequence[float]) -> float:
   26:     if not values:
   27:         return 0.0
   28:     return float(fmean(values))
   29: 
   30: 
   31: @dataclass(frozen=True, slots=True)
   32: class StrategyPerformancePoint:
   33:     """One reviewed prediction used by bounded rolling-window calculations."""
   34: 
   35:     prediction_id: str
   36:     round_no: int
   37:     match_count: int
   38:     prediction_score: float
   39:     prize_rank: int | None
   40: 
   41:     def __post_init__(self) -> None:
   42:         if not self.prediction_id.strip():
   43:             raise ValueError("prediction_id must not be empty")
   44:         if self.round_no <= 0:
   45:             raise ValueError("round_no must be positive")
   46:         if self.match_count < 0 or self.match_count > 6:
   47:             raise ValueError("match_count must be between 0 and 6")
   48:         if not 0.0 <= float(self.prediction_score) <= 1.0:
   49:             raise ValueError("prediction_score must be between 0 and 1")
   50:         if self.prize_rank is not None and not 1 <= self.prize_rank <= 5:
   51:             raise ValueError("prize_rank must be 1 to 5 or None")
   52: 
   53: 
   54: @dataclass(frozen=True, slots=True)
   55: class RankingWeights:
   56:     """Weights used to build one normalized rank score."""
   57: 
   58:     performance: float = 0.42
   59:     recent_gain: float = 0.18
   60:     prize_rate: float = 0.12
   61:     stability: float = 0.10
   62:     confidence: float = 0.10
   63:     prediction_quality: float = 0.08
   64: 
   65:     def __post_init__(self) -> None:
   66:         values = (
   67:             self.performance,
   68:             self.recent_gain,
   69:             self.prize_rate,
   70:             self.stability,
   71:             self.confidence,
   72:             self.prediction_quality,
   73:         )
   74:         if any(value < 0.0 for value in values):
   75:             raise ValueError("ranking weights must be non-negative")
   76:         if not math.isclose(sum(values), 1.0, abs_tol=1e-9):
   77:             raise ValueError("ranking weights must sum to 1.0")
   78: 
   79: 
   80: @dataclass(frozen=True, slots=True)
   81: class RankingConfig:
   82:     """Deterministic ranking parameters."""
   83: 
   84:     windows: tuple[int, ...] = (10, 20, 50, 100)
   85:     trend_short_window: int = 10
   86:     trend_long_window: int = 30
   87:     trend_threshold: float = 0.20
   88:     confidence_scale: float = 20.0
   89:     weights: RankingWeights = field(default_factory=RankingWeights)
   90: 
   91:     def __post_init__(self) -> None:
   92:         if not self.windows:
   93:             raise ValueError("windows must not be empty")
   94:         if any(window <= 0 for window in self.windows):
   95:             raise ValueError("windows must contain positive integers")
   96:         if len(set(self.windows)) != len(self.windows):
   97:             raise ValueError("windows must not contain duplicates")
   98:         if self.trend_short_window <= 0 or self.trend_long_window <= 0:
   99:             raise ValueError("trend windows must be positive")
  100:         if self.trend_short_window > self.trend_long_window:
  101:             raise ValueError(
  102:                 "trend_short_window must be less than or equal to "
  103:                 "trend_long_window"
  104:             )
  105:         if self.trend_threshold < 0.0:
  106:             raise ValueError("trend_threshold must be non-negative")
  107:         if self.confidence_scale <= 0.0:
  108:             raise ValueError("confidence_scale must be positive")
  109: 
  110: 
  111: @dataclass(frozen=True, slots=True)
  112: class StrategyRanking:
  113:     """Derived ranking output for one model or scenario strategy."""
  114: 
  115:     strategy_type: str
  116:     strategy_name: str
  117:     rank_position: int
  118:     rank_score: float
  119:     confidence: float
  120:     stability: float
  121:     trend: str
  122:     recent_gain: float
  123:     sample_count: int
  124:     average_match_count: float
  125:     average_prediction_score: float
  126:     prize_rate: float
  127:     rolling_matches: Mapping[int, float]
  128:     rolling_prize_rates: Mapping[int, float]
  129: 
  130:     def __post_init__(self) -> None:
  131:         if not self.strategy_type.strip():
  132:             raise ValueError("strategy_type must not be empty")
  133:         if not self.strategy_name.strip():
  134:             raise ValueError("strategy_name must not be empty")
  135:         if self.rank_position <= 0:
  136:             raise ValueError("rank_position must be positive")
  137:         if self.trend not in _VALID_TRENDS:
  138:             raise ValueError("trend must be UP, DOWN, or FLAT")
  139: 
  140:     def as_dict(self) -> dict[str, object]:
  141:         return {
  142:             "strategy_type": self.strategy_type,
  143:             "strategy_name": self.strategy_name,
  144:             "rank_position": self.rank_position,
  145:             "rank_score": round(self.rank_score, 6),
  146:             "confidence": round(self.confidence, 6),
  147:             "stability": round(self.stability, 6),
  148:             "trend": self.trend,
  149:             "recent_gain": round(self.recent_gain, 6),
  150:             "sample_count": self.sample_count,
  151:             "average_match_count": round(self.average_match_count, 6),
  152:             "average_prediction_score": round(
  153:                 self.average_prediction_score,
  154:                 6,
  155:             ),
  156:             "prize_rate": round(self.prize_rate, 6),
  157:             "rolling_matches": {
  158:                 str(window): round(value, 6)
  159:                 for window, value in sorted(self.rolling_matches.items())
  160:             },
  161:             "rolling_prize_rates": {
  162:                 str(window): round(value, 6)
  163:                 for window, value in sorted(
  164:                     self.rolling_prize_rates.items()
  165:                 )
  166:             },
  167:         }
  168: 
  169: 
  170: class StrategyRankingEngine:
  171:     """Build deterministic, normalized rankings from bounded history."""
  172: 
  173:     def __init__(
  174:         self,
  175:         config: RankingConfig | None = None,
  176:     ) -> None:
  177:         self.config = config or RankingConfig()
  178: 
  179:     def rank(
  180:         self,
  181:         statistics: Sequence[StrategyStatistics],
  182:         histories: Mapping[
  183:             tuple[str, str],
  184:             Sequence[StrategyPerformancePoint],
  185:         ],
  186:     ) -> tuple[StrategyRanking, ...]:
  187:         """Rank strategies, then assign stable one-based positions."""
  188: 
  189:         provisional: list[dict[str, object]] = []
  190: 
  191:         for item in statistics:
  192:             key = (item.strategy_type, item.strategy_name)
  193:             history = tuple(histories.get(key, ()))
  194:             provisional.append(
  195:                 self._calculate_one(
  196:                     statistics=item,
  197:                     history=history,
  198:                 )
  199:             )
  200: 
  201:         provisional.sort(
  202:             key=lambda row: (
  203:                 -float(row["rank_score"]),
  204:                 -float(row["confidence"]),
  205:                 -int(row["sample_count"]),
  206:                 str(row["strategy_type"]),
  207:                 str(row["strategy_name"]),
  208:             )
  209:         )
  210: 
  211:         return tuple(
  212:             StrategyRanking(
  213:                 rank_position=index,
  214:                 **row,
  215:             )
  216:             for index, row in enumerate(provisional, start=1)
  217:         )
  218: 
  219:     def _calculate_one(
  220:         self,
  221:         *,
  222:         statistics: StrategyStatistics,
  223:         history: Sequence[StrategyPerformancePoint],
  224:     ) -> dict[str, object]:
  225:         ordered_history = tuple(
  226:             sorted(
  227:                 history,
  228:                 key=lambda point: (
  229:                     point.round_no,
  230:                     point.prediction_id,
  231:                 ),
  232:             )
  233:         )
  234: 
  235:         match_values = [
  236:             float(point.match_count)
  237:             for point in ordered_history
  238:         ]
  239:         prize_values = [
  240:             1.0 if point.prize_rank is not None else 0.0
  241:             for point in ordered_history
  242:         ]
  243: 
  244:         rolling_matches = {
  245:             window: _safe_mean(match_values[-window:])
  246:             for window in self.config.windows
  247:         }
  248:         rolling_prize_rates = {
  249:             window: _safe_mean(prize_values[-window:])
  250:             for window in self.config.windows
  251:         }
  252: 
  253:         confidence = self._confidence(statistics.sample_count)
  254:         stability = self._stability(match_values)
  255:         trend, recent_gain = self._trend(match_values)
  256: 
  257:         performance_component = _clamp(
  258:             statistics.average_match_count / 6.0
  259:         )
  260:         recent_component = _clamp(
  261:             0.5 + (recent_gain / 6.0)
  262:         )
  263:         prize_component = _clamp(statistics.prize_rate)
  264:         prediction_quality_component = _clamp(
  265:             statistics.average_prediction_score
  266:         )
  267: 
  268:         weights = self.config.weights
  269:         rank_score = _clamp(
  270:             weights.performance * performance_component
  271:             + weights.recent_gain * recent_component
  272:             + weights.prize_rate * prize_component
  273:             + weights.stability * stability
  274:             + weights.confidence * confidence
  275:             + weights.prediction_quality
  276:             * prediction_quality_component
  277:         )
  278: 
  279:         return {
  280:             "strategy_type": statistics.strategy_type,
  281:             "strategy_name": statistics.strategy_name,
  282:             "rank_score": rank_score,
  283:             "confidence": confidence,
  284:             "stability": stability,
  285:             "trend": trend,
  286:             "recent_gain": recent_gain,
  287:             "sample_count": statistics.sample_count,
  288:             "average_match_count": statistics.average_match_count,
  289:             "average_prediction_score": (
  290:                 statistics.average_prediction_score
  291:             ),
  292:             "prize_rate": statistics.prize_rate,
  293:             "rolling_matches": rolling_matches,
  294:             "rolling_prize_rates": rolling_prize_rates,
  295:         }
  296: 
  297:     def _confidence(self, sample_count: int) -> float:
  298:         if sample_count <= 0:
  299:             return 0.0
  300: 
  301:         # Smoothly approaches 1.0 without allowing tiny samples to dominate.
  302:         return _clamp(
  303:             1.0 - math.exp(
  304:                 -float(sample_count)
  305:                 / self.config.confidence_scale
  306:             )
  307:         )
  308: 
  309:     @staticmethod
  310:     def _stability(match_values: Sequence[float]) -> float:
  311:         if not match_values:
  312:             return 0.0
  313:         if len(match_values) == 1:
  314:             return 0.5
  315: 
  316:         deviation = float(pstdev(match_values))
  317:         # Match counts are bounded to 0..6.  A deviation of three or more is
  318:         # treated as maximally unstable.
  319:         return _clamp(1.0 - deviation / 3.0)
  320: 
  321:     def _trend(
  322:         self,
  323:         match_values: Sequence[float],
  324:     ) -> tuple[str, float]:
  325:         if not match_values:
  326:             return "FLAT", 0.0
  327: 
  328:         short_values = match_values[
  329:             -self.config.trend_short_window:
  330:         ]
  331:         long_values = match_values[
  332:             -self.config.trend_long_window:
  333:         ]
  334: 
  335:         short_average = _safe_mean(short_values)
  336:         long_average = _safe_mean(long_values)
  337:         recent_gain = short_average - long_average
  338: 
  339:         if recent_gain > self.config.trend_threshold:
  340:             return "UP", recent_gain
  341:         if recent_gain < -self.config.trend_threshold:
  342:             return "DOWN", recent_gain
  343:         return "FLAT", recent_gain


########################################################################################
FILE: lrp/learning/ranking_repository.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------


class RankingDataset
  bases: (none)
  decorators: dataclass(frozen=True, slots=True)
  method: strategy_count(self) -> int
  method: history_point_count(self) -> int

class RankingRepository
  bases: (none)
  decorators: (none)
  method: __init__(self, repository: LearningRepository) -> None
  method: repository_revision(self) -> tuple[int, int]
  method: get_strategy_history(self, *, strategy_type: str, strategy_name: str, limit: int = 100) -> tuple[StrategyPerformancePoint, ...]
  method: build_dataset(self, *, strategy_type: str | None = None, history_limit: int = 100) -> RankingDataset
  method: invalidate_cache(self) -> None
  method: _read_connection(self) -> Iterator[sqlite3.Connection]
  method: _required_text(value: str, *, field_name: str) -> str

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Read-optimized data source for the M6-004 ranking engine.
    2: 
    3: This module deliberately avoids changing the append-only learning schema.
    4: It composes the existing public LearningRepository statistics API with
    5: bounded, read-only SQLite queries over strategy events.
    6: 
    7: The cache is revision-aware: a new strategy event changes the repository
    8: revision and invalidates cached ranking datasets automatically.
    9: """
   10: 
   11: from __future__ import annotations
   12: 
   13: from contextlib import contextmanager
   14: from dataclasses import dataclass
   15: import sqlite3
   16: from typing import Iterator, Mapping, Sequence
   17: 
   18: from .ranking import StrategyPerformancePoint
   19: from .repository import LearningRepository
   20: from .strategy_stats import StrategyStatistics
   21: 
   22: 
   23: StrategyKey = tuple[str, str]
   24: 
   25: 
   26: @dataclass(frozen=True, slots=True)
   27: class RankingDataset:
   28:     """Immutable input bundle consumed by StrategyRankingEngine."""
   29: 
   30:     revision: tuple[int, int]
   31:     statistics: tuple[StrategyStatistics, ...]
   32:     histories: Mapping[
   33:         StrategyKey,
   34:         tuple[StrategyPerformancePoint, ...],
   35:     ]
   36: 
   37:     @property
   38:     def strategy_count(self) -> int:
   39:         return len(self.statistics)
   40: 
   41:     @property
   42:     def history_point_count(self) -> int:
   43:         return sum(
   44:             len(points)
   45:             for points in self.histories.values()
   46:         )
   47: 
   48: 
   49: class RankingRepository:
   50:     """Build and cache bounded ranking datasets from learning records."""
   51: 
   52:     def __init__(
   53:         self,
   54:         repository: LearningRepository,
   55:     ) -> None:
   56:         self.repository = repository
   57:         self._cache_key: tuple[
   58:             tuple[int, int],
   59:             str | None,
   60:             int,
   61:         ] | None = None
   62:         self._cache_value: RankingDataset | None = None
   63: 
   64:     def repository_revision(self) -> tuple[int, int]:
   65:         """Return a cheap monotonic fingerprint for strategy events.
   66: 
   67:         The tuple is ``(event_count, maximum_rowid)``.  Because strategy
   68:         events are append-only, either value changes whenever new ranking
   69:         evidence is aggregated.
   70:         """
   71: 
   72:         self.repository.initialize()
   73: 
   74:         with self._read_connection() as connection:
   75:             row = connection.execute(
   76:                 """
   77:                 SELECT
   78:                     COUNT(*) AS event_count,
   79:                     COALESCE(MAX(rowid), 0) AS maximum_rowid
   80:                 FROM learning_strategy_events
   81:                 """
   82:             ).fetchone()
   83: 
   84:         return (
   85:             int(row["event_count"]),
   86:             int(row["maximum_rowid"]),
   87:         )
   88: 
   89:     def get_strategy_history(
   90:         self,
   91:         *,
   92:         strategy_type: str,
   93:         strategy_name: str,
   94:         limit: int = 100,
   95:     ) -> tuple[StrategyPerformancePoint, ...]:
   96:         """Return the newest bounded history in chronological order."""
   97: 
   98:         normalized_type = self._required_text(
   99:             strategy_type,
  100:             field_name="strategy_type",
  101:         )
  102:         normalized_name = self._required_text(
  103:             strategy_name,
  104:             field_name="strategy_name",
  105:         )
  106: 
  107:         if limit <= 0:
  108:             raise ValueError("limit must be positive")
  109: 
  110:         self.repository.initialize()
  111: 
  112:         with self._read_connection() as connection:
  113:             rows = connection.execute(
  114:                 """
  115:                 SELECT
  116:                     event.prediction_id,
  117:                     prediction.round_no,
  118:                     review.match_count,
  119:                     prediction.score AS prediction_score,
  120:                     review.prize_rank
  121:                 FROM learning_strategy_events AS event
  122:                 INNER JOIN learning_predictions AS prediction
  123:                     ON prediction.prediction_id =
  124:                        event.prediction_id
  125:                 INNER JOIN learning_reviews AS review
  126:                     ON review.prediction_id =
  127:                        event.prediction_id
  128:                 WHERE event.strategy_type = ?
  129:                   AND event.strategy_name = ?
  130:                 ORDER BY
  131:                     prediction.round_no DESC,
  132:                     event.prediction_id DESC
  133:                 LIMIT ?
  134:                 """,
  135:                 (
  136:                     normalized_type,
  137:                     normalized_name,
  138:                     int(limit),
  139:                 ),
  140:             ).fetchall()
  141: 
  142:         # SQL reads newest first for an indexed bounded query.  The engine
  143:         # receives chronological order to make rolling/trend semantics clear.
  144:         return tuple(
  145:             StrategyPerformancePoint(
  146:                 prediction_id=str(row["prediction_id"]),
  147:                 round_no=int(row["round_no"]),
  148:                 match_count=int(row["match_count"]),
  149:                 prediction_score=float(
  150:                     row["prediction_score"]
  151:                 ),
  152:                 prize_rank=(
  153:                     None
  154:                     if row["prize_rank"] is None
  155:                     else int(row["prize_rank"])
  156:                 ),
  157:             )
  158:             for row in reversed(rows)
  159:         )
  160: 
  161:     def build_dataset(
  162:         self,
  163:         *,
  164:         strategy_type: str | None = None,
  165:         history_limit: int = 100,
  166:     ) -> RankingDataset:
  167:         """Build or reuse a complete ranking-engine input dataset."""
  168: 
  169:         if history_limit <= 0:
  170:             raise ValueError("history_limit must be positive")
  171: 
  172:         normalized_type = (
  173:             None
  174:             if strategy_type is None
  175:             else self._required_text(
  176:                 strategy_type,
  177:                 field_name="strategy_type",
  178:             )
  179:         )
  180: 
  181:         revision = self.repository_revision()
  182:         cache_key = (
  183:             revision,
  184:             normalized_type,
  185:             int(history_limit),
  186:         )
  187: 
  188:         if (
  189:             self._cache_key == cache_key
  190:             and self._cache_value is not None
  191:         ):
  192:             return self._cache_value
  193: 
  194:         statistics = self.repository.get_strategy_statistics(
  195:             strategy_type=normalized_type
  196:         )
  197: 
  198:         histories: dict[
  199:             StrategyKey,
  200:             tuple[StrategyPerformancePoint, ...],
  201:         ] = {}
  202: 
  203:         for item in statistics:
  204:             key = (
  205:                 item.strategy_type,
  206:                 item.strategy_name,
  207:             )
  208:             histories[key] = self.get_strategy_history(
  209:                 strategy_type=item.strategy_type,
  210:                 strategy_name=item.strategy_name,
  211:                 limit=history_limit,
  212:             )
  213: 
  214:         dataset = RankingDataset(
  215:             revision=revision,
  216:             statistics=statistics,
  217:             histories=histories,
  218:         )
  219: 
  220:         self._cache_key = cache_key
  221:         self._cache_value = dataset
  222:         return dataset
  223: 
  224:     def invalidate_cache(self) -> None:
  225:         """Explicitly clear the local read cache."""
  226: 
  227:         self._cache_key = None
  228:         self._cache_value = None
  229: 
  230:     @contextmanager
  231:     def _read_connection(
  232:         self,
  233:     ) -> Iterator[sqlite3.Connection]:
  234:         """Open and always close a short-lived read connection.
  235: 
  236:         ``sqlite3.Connection`` used directly as a context manager commits
  237:         or rolls back a transaction, but it does not close the connection.
  238:         Explicit cleanup is required so Windows can remove temporary
  239:         database files immediately after a regression test.
  240:         """
  241: 
  242:         connection = sqlite3.connect(
  243:             self.repository.database_path,
  244:             timeout=10.0,
  245:         )
  246:         connection.row_factory = sqlite3.Row
  247:         connection.execute("PRAGMA foreign_keys = ON")
  248: 
  249:         try:
  250:             yield connection
  251:         finally:
  252:             connection.close()
  253: 
  254:     @staticmethod
  255:     def _required_text(
  256:         value: str,
  257:         *,
  258:         field_name: str,
  259:     ) -> str:
  260:         normalized = str(value).strip()
  261:         if not normalized:
  262:             raise ValueError(
  263:                 f"{field_name} must not be empty"
  264:             )
  265:         return normalized


########################################################################################
FILE: lrp/learning/__init__.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------

(no public AST contracts found)

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Public learning package API."""
    2: 
    3: from .adaptive_engine import (
    4:     AdaptiveWeightConfig,
    5:     AdaptiveWeightEngine,
    6:     AdaptiveWeightFactors,
    7: )
    8: from .adaptive_models import (
    9:     AdaptiveRevision,
   10:     AdaptiveWeight,
   11:     AdaptiveWeightDataset,
   12: )
   13: from .adaptive_repository import AdaptiveWeightRepository
   14: from .aggregator import (
   15:     StrategyAggregationSummary,
   16:     StrategyAggregator,
   17: )
   18: from .evaluator import (
   19:     determine_prize_rank,
   20:     evaluate_prediction,
   21: )
   22: from .learning_facade import LearningFacade
   23: from .models import (
   24:     PredictionRecord,
   25:     ResultRecord,
   26:     ReviewRecord,
   27: )
   28: from .performance import (
   29:     PerformanceAnalyzer,
   30:     StrategyPerformanceReport,
   31:     StrategyPerformanceSummary,
   32: )
   33: from .ranking import (
   34:     RankingConfig,
   35:     RankingWeights,
   36:     StrategyPerformancePoint,
   37:     StrategyRanking,
   38:     StrategyRankingEngine,
   39: )
   40: from .ranking_repository import (
   41:     RankingDataset,
   42:     RankingRepository,
   43: )
   44: from .repository import LearningRepository
   45: from .service import (
   46:     IncrementalReviewSummary,
   47:     LearningService,
   48: )
   49: from .strategy_stats import StrategyStatistics
   50: 
   51: 
   52: __all__ = [
   53:     "AdaptiveRevision",
   54:     "AdaptiveWeight",
   55:     "AdaptiveWeightConfig",
   56:     "AdaptiveWeightDataset",
   57:     "AdaptiveWeightEngine",
   58:     "AdaptiveWeightFactors",
   59:     "AdaptiveWeightRepository",
   60:     "IncrementalReviewSummary",
   61:     "LearningFacade",
   62:     "LearningRepository",
   63:     "LearningService",
   64:     "PerformanceAnalyzer",
   65:     "PredictionRecord",
   66:     "RankingConfig",
   67:     "RankingDataset",
   68:     "RankingRepository",
   69:     "RankingWeights",
   70:     "ResultRecord",
   71:     "ReviewRecord",
   72:     "StrategyAggregationSummary",
   73:     "StrategyAggregator",
   74:     "StrategyPerformancePoint",
   75:     "StrategyPerformanceReport",
   76:     "StrategyPerformanceSummary",
   77:     "StrategyRanking",
   78:     "StrategyRankingEngine",
   79:     "StrategyStatistics",
   80:     "determine_prize_rank",
   81:     "evaluate_prediction",
   82: ]


########################################################################################
FILE: tests/test_m6_adaptive_weights.py
########################################################################################

STATUS: MISSING


########################################################################################
FILE: tests/test_e005a_performance_analyzer.py
########################################################################################

STATUS: FOUND

----------------------------------------------------------------------------------------
AST CONTRACT SUMMARY
----------------------------------------------------------------------------------------

def make_prediction(*, round_no: int, model_name: str, set_id: str, numbers: tuple[int, ...], score: float, scenario: str) -> PredictionRecord
def add_round(*, repository: LearningRepository, round_no: int, winning_numbers: tuple[int, ...], predictions: tuple[PredictionRecord, ...]) -> None
def test_empty_report() -> None
def test_performance_report() -> None
def test_all_strategy_types() -> None
def main() -> None

----------------------------------------------------------------------------------------
FULL SOURCE
----------------------------------------------------------------------------------------

    1: """Regression tests for Project E E-005A Performance Analyzer."""
    2: 
    3: from __future__ import annotations
    4: 
    5: from pathlib import Path
    6: import tempfile
    7: 
    8: from lrp.learning import (
    9:     LearningRepository,
   10:     LearningService,
   11:     PerformanceAnalyzer,
   12:     PredictionRecord,
   13:     RankingConfig,
   14:     ResultRecord,
   15:     StrategyPerformanceReport,
   16:     StrategyPerformanceSummary,
   17: )
   18: 
   19: 
   20: GENERATED_AT_KST = "2026-07-27T20:00:00+09:00"
   21: RECORDED_AT_KST = "2026-07-27T21:00:00+09:00"
   22: REVIEWED_AT_KST = "2026-07-27T21:01:00+09:00"
   23: AGGREGATED_AT_KST = "2026-07-27T21:02:00+09:00"
   24: REPORT_AT_KST = "2026-07-27T21:03:00+09:00"
   25: 
   26: 
   27: def make_prediction(
   28:     *,
   29:     round_no: int,
   30:     model_name: str,
   31:     set_id: str,
   32:     numbers: tuple[int, ...],
   33:     score: float,
   34:     scenario: str,
   35: ) -> PredictionRecord:
   36:     return PredictionRecord(
   37:         prediction_id=f"{round_no}:{model_name}:{set_id}",
   38:         round_no=round_no,
   39:         set_id=set_id,
   40:         numbers=numbers,
   41:         score=score,
   42:         model_name=model_name,
   43:         seed=20260727,
   44:         generated_at_kst=GENERATED_AT_KST,
   45:         features={
   46:             "gap_mix": 0.70,
   47:             "pair_affinity": 0.60,
   48:         },
   49:         parameters={
   50:             "temperature": 0.85,
   51:             "scenario": scenario,
   52:         },
   53:     )
   54: 
   55: 
   56: def add_round(
   57:     *,
   58:     repository: LearningRepository,
   59:     round_no: int,
   60:     winning_numbers: tuple[int, ...],
   61:     predictions: tuple[PredictionRecord, ...],
   62: ) -> None:
   63:     for prediction in predictions:
   64:         assert repository.add_prediction(prediction) is True
   65: 
   66:     bonus = next(
   67:         number
   68:         for number in range(1, 46)
   69:         if number not in winning_numbers
   70:     )
   71: 
   72:     assert repository.add_result(
   73:         ResultRecord(
   74:             round_no=round_no,
   75:             numbers=winning_numbers,
   76:             bonus=bonus,
   77:             recorded_at_kst=RECORDED_AT_KST,
   78:         )
   79:     ) is True
   80: 
   81: 
   82: def test_empty_report() -> None:
   83:     with tempfile.TemporaryDirectory() as temporary:
   84:         repository = LearningRepository(
   85:             Path(temporary) / "learning.db"
   86:         )
   87:         service = LearningService(repository)
   88:         analyzer = PerformanceAnalyzer(
   89:             service.ranking_repository
   90:         )
   91: 
   92:         report = analyzer.analyze(
   93:             strategy_type="model",
   94:             history_limit=10,
   95:             generated_at_kst=REPORT_AT_KST,
   96:         )
   97: 
   98:         assert isinstance(
   99:             report,
  100:             StrategyPerformanceReport,
  101:         )
  102:         assert report.revision == (0, 0)
  103:         assert report.strategy_count == 0
  104:         assert report.total_samples == 0
  105:         assert report.summaries == ()
  106:         assert report.metadata["read_only"] is True
  107: 
  108: 
  109: def test_performance_report() -> None:
  110:     with tempfile.TemporaryDirectory() as temporary:
  111:         repository = LearningRepository(
  112:             Path(temporary) / "learning.db"
  113:         )
  114:         service = LearningService(repository)
  115: 
  116:         rounds = (
  117:             (
  118:                 1220,
  119:                 (1, 2, 3, 4, 5, 6),
  120:                 (1, 2, 20, 21, 22, 23),
  121:                 (1, 2, 3, 20, 21, 22),
  122:             ),
  123:             (
  124:                 1221,
  125:                 (10, 11, 12, 13, 14, 15),
  126:                 (10, 11, 12, 30, 31, 32),
  127:                 (10, 11, 30, 31, 32, 33),
  128:             ),
  129:             (
  130:                 1222,
  131:                 (20, 21, 22, 23, 24, 25),
  132:                 (20, 21, 22, 23, 30, 31),
  133:                 (20, 21, 32, 33, 34, 35),
  134:             ),
  135:             (
  136:                 1223,
  137:                 (30, 31, 32, 33, 34, 35),
  138:                 (30, 31, 32, 33, 34, 40),
  139:                 (30, 31, 42, 43, 44, 45),
  140:             ),
  141:         )
  142: 
  143:         for (
  144:             round_no,
  145:             winning,
  146:             gpt_numbers,
  147:             gemini_numbers,
  148:         ) in rounds:
  149:             add_round(
  150:                 repository=repository,
  151:                 round_no=round_no,
  152:                 winning_numbers=winning,
  153:                 predictions=(
  154:                     make_prediction(
  155:                         round_no=round_no,
  156:                         model_name="GPT-v3.3",
  157:                         set_id="S1",
  158:                         numbers=gpt_numbers,
  159:                         score=0.90,
  160:                         scenario="gap",
  161:                     ),
  162:                     make_prediction(
  163:                         round_no=round_no,
  164:                         model_name="Gemini-v7.1",
  165:                         set_id="S1",
  166:                         numbers=gemini_numbers,
  167:                         score=0.82,
  168:                         scenario="pair",
  169:                     ),
  170:                 ),
  171:             )
  172: 
  173:         review = service.run_incremental_review(
  174:             reviewed_at_kst=REVIEWED_AT_KST
  175:         )
  176:         assert review.scanned == 8
  177:         assert review.created == 8
  178: 
  179:         aggregation = service.run_strategy_aggregation(
  180:             aggregated_at_kst=AGGREGATED_AT_KST
  181:         )
  182:         assert aggregation.scanned == 8
  183:         assert aggregation.created_events == 16
  184: 
  185:         before_counts = repository.counts()
  186: 
  187:         analyzer = PerformanceAnalyzer(
  188:             service.ranking_repository,
  189:             ranking_config=RankingConfig(
  190:                 windows=(2, 4),
  191:                 trend_short_window=2,
  192:                 trend_long_window=4,
  193:                 trend_threshold=0.20,
  194:                 confidence_scale=4.0,
  195:             ),
  196:         )
  197: 
  198:         report = analyzer.analyze(
  199:             strategy_type="model",
  200:             history_limit=3,
  201:             generated_at_kst=REPORT_AT_KST,
  202:         )
  203: 
  204:         after_counts = repository.counts()
  205:         assert before_counts == after_counts
  206: 
  207:         assert isinstance(
  208:             report,
  209:             StrategyPerformanceReport,
  210:         )
  211:         assert report.revision == (16, 16)
  212:         assert report.strategy_type == "model"
  213:         assert report.history_limit == 3
  214:         assert report.generated_at_kst == REPORT_AT_KST
  215:         assert report.strategy_count == 2
  216:         assert report.total_samples == 8
  217:         assert report.total_history_points == 6
  218: 
  219:         gpt = report.get("model", "GPT-v3.3")
  220:         gemini = report.get("model", "Gemini-v7.1")
  221: 
  222:         assert isinstance(
  223:             gpt,
  224:             StrategyPerformanceSummary,
  225:         )
  226:         assert isinstance(
  227:             gemini,
  228:             StrategyPerformanceSummary,
  229:         )
  230: 
  231:         assert gpt.sample_count == 4
  232:         assert gpt.history_count == 3
  233:         assert [
  234:             point.round_no
  235:             for point in gpt.history
  236:         ] == [1221, 1222, 1223]
  237:         assert [
  238:             point.match_count
  239:             for point in gpt.history
  240:         ] == [3, 4, 5]
  241:         assert gpt.best_match_count == 5
  242:         assert gpt.worst_match_count == 3
  243:         assert gpt.average_match_count == 3.5
  244:         assert gpt.average_prediction_score == 0.90
  245:         assert gpt.hit3_plus_rate == 0.75
  246:         assert gpt.prize_rate == 0.75
  247:         assert gpt.trend == "UP"
  248:         assert gpt.recent_gain > 0.0
  249:         assert gpt.rank_position == 1
  250: 
  251:         assert gemini.sample_count == 4
  252:         assert gemini.history_count == 3
  253:         assert [
  254:             point.round_no
  255:             for point in gemini.history
  256:         ] == [1221, 1222, 1223]
  257:         assert [
  258:             point.match_count
  259:             for point in gemini.history
  260:         ] == [2, 2, 2]
  261:         assert gemini.best_match_count == 2
  262:         assert gemini.worst_match_count == 2
  263:         assert gemini.trend == "FLAT"
  264:         assert gemini.rank_position == 2
  265: 
  266:         assert gpt.rank_score > gemini.rank_score
  267:         assert 0.0 <= gpt.confidence <= 1.0
  268:         assert 0.0 <= gpt.stability <= 1.0
  269: 
  270:         payload = report.as_dict(
  271:             include_history=True
  272:         )
  273:         assert payload["strategy_count"] == 2
  274:         assert payload["revision"] == [16, 16]
  275:         assert len(
  276:             payload["summaries"][0]["history"]
  277:         ) == 3
  278: 
  279:         compact = report.as_dict(
  280:             include_history=False
  281:         )
  282:         assert "history" not in compact["summaries"][0]
  283: 
  284:         second = analyzer.analyze(
  285:             strategy_type="model",
  286:             history_limit=3,
  287:             generated_at_kst=REPORT_AT_KST,
  288:         )
  289:         assert second == report
  290: 
  291: 
  292: def test_all_strategy_types() -> None:
  293:     with tempfile.TemporaryDirectory() as temporary:
  294:         repository = LearningRepository(
  295:             Path(temporary) / "learning.db"
  296:         )
  297:         service = LearningService(repository)
  298: 
  299:         add_round(
  300:             repository=repository,
  301:             round_no=1230,
  302:             winning_numbers=(1, 2, 3, 4, 5, 6),
  303:             predictions=(
  304:                 make_prediction(
  305:                     round_no=1230,
  306:                     model_name="GPT-v3.3",
  307:                     set_id="S1",
  308:                     numbers=(1, 2, 3, 4, 20, 21),
  309:                     score=0.88,
  310:                     scenario="gap",
  311:                 ),
  312:             ),
  313:         )
  314: 
  315:         service.run_incremental_review(
  316:             reviewed_at_kst=REVIEWED_AT_KST
  317:         )
  318:         service.run_strategy_aggregation(
  319:             aggregated_at_kst=AGGREGATED_AT_KST
  320:         )
  321: 
  322:         analyzer = PerformanceAnalyzer(
  323:             service.ranking_repository
  324:         )
  325:         report = analyzer.analyze(
  326:             history_limit=10,
  327:             generated_at_kst=REPORT_AT_KST,
  328:         )
  329: 
  330:         assert report.strategy_type is None
  331:         assert report.strategy_count == 2
  332:         assert {
  333:             item.key
  334:             for item in report.summaries
  335:         } == {
  336:             ("model", "GPT-v3.3"),
  337:             ("scenario", "gap"),
  338:         }
  339: 
  340: 
  341: def main() -> None:
  342:     test_empty_report()
  343:     test_performance_report()
  344:     test_all_strategy_types()
  345: 
  346:     print(
  347:         "PASS: Project E E-005A performance analyzer"
  348:     )
  349:     print("read_only_analysis: PASS")
  350:     print("repository_revision: PASS")
  351:     print("bounded_history: PASS")
  352:     print("chronological_history: PASS")
  353:     print("confidence_stability_trend: PASS")
  354:     print("model_scenario_reports: PASS")
  355:     print("deterministic_report: PASS")
  356:     print("public_api_compatibility: PASS")
  357: 
  358: 
  359: if __name__ == "__main__":
  360:     main()
